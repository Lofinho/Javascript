// Faça um Programa que peça o raio de um círculo, calcule e mostre sua área.

let radius = Number(prompt('Input the radius circle value '))
const PI = 3.14
let area = PI * radius**2

alert(`The radius circle is ${area}`)